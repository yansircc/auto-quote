export interface Dimensions {
  width: number; // x轴
  depth: number; // y轴
  height: number; // z轴
}

export interface BaseProduct {
  materialName: string;
}

export interface BaseMold {
  materialName: string;
}
