/**
 * 使用三分搜索或其他方法在穴数范围内寻找最优值（示意）
 * @param {function} costFunction 给定穴数返回总成本的函数
 * @param {number} left 左边界
 * @param {number} right 右边界
 * @returns {number} 最优穴数
 */
export function ternarySearchForOptimalCavity(
  costFunction: (cavity: number) => number,
  left: number,
  right: number,
): number {
  // 伪代码
  return 0;
}
